<%= @task.details %>

<%= link_to 'Home', tasks_path %>
<%= link_to "Edit the task", edit_task_path(@task) %>
<%= link_to 'Delete',
  task_path(@task),
  method: :delete,
  data: { confirm: 'Are you sure?' } %>
